{
  "name": "pastokima-owner-dashboard",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs && node build-dashboard.mjs",
    "test": "node --test tests/*.test.mjs",
    "db:generate": "drizzle-kit generate",
    "build:vercel": "npm run build && node build-vercel.mjs"
  },
  "devDependencies": {
    "drizzle-kit": "0.31.10",
    "drizzle-orm": "0.45.2",
    "esbuild": "0.25.12",
    "miniflare": "4.20260515.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.116.0"
  },
  "engines": {
    "node": ">=22"
  }
}
